name: Build Desktop Executables

on:
  push:
    tags:
      - 'v*' # Runs when you push a version tag like v1.0.0
  workflow_dispatch: # Allows manual triggering from the GitHub Actions tab

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-14, windows-latest]
        include:
          - os: ubuntu-22.04
            artifact_name: AI-DataAnalysisApp-linux
            pyinstaller_args: --add-data "frontend:frontend"
          - os: macos-14
            artifact_name: AI-DataAnalysisApp-macos
            pyinstaller_args: --add-data "frontend:frontend"
          - os: windows-latest
            artifact_name: AI-DataAnalysisApp-windows.exe
            pyinstaller_args: --add-data "frontend;frontend"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --onefile --noconsole ${{ matrix.pyinstaller_args }} --name "AI-DataAnalysisApp" launcher.py

      - name: Rename executable on Unix (non-Windows)
        if: matrix.os != 'windows-latest'
        run: |
          mv dist/AI-DataAnalysisApp dist/${{ matrix.artifact_name }}

      - name: Rename executable on Windows
        if: matrix.os == 'windows-latest'
        run: |
          mv dist/AI-DataAnalysisApp.exe dist/${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') # Only run if tag is pushed
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/AI-DataAnalysisApp-windows.exe/AI-DataAnalysisApp-windows.exe
            artifacts/AI-DataAnalysisApp-macos/AI-DataAnalysisApp-macos
            artifacts/AI-DataAnalysisApp-linux/AI-DataAnalysisApp-linux
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
